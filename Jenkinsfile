pipeline {
    agent any 

  

    stages {

        stage('Create BaseLine Jobs') {
            steps {
                script {
                    

                    // poc pipeline
                    load("jobs/bucket/s3bucket.groovy")
                
                }
            }
        }
    }
}
